import React from 'react';

function Dahboard(){
    var token = localStorage.getItem('accessToken');
    console.log(token);
    return(
        <div>
            lala
        </div>
    );
}

export default Dahboard;